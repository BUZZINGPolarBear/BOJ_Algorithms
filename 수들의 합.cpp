#include <iostream>
#include <algorithm>
#include <vector>
#include <string>
#include <queue>
using namespace std;

//long long a[10000000] = { 0, };
//long long a[10005] = { 0, };

int main()
{
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);

	long long n, ans = 0, sum = 0, cnt = 0;
	long long left = 1, right = 1;

	cin >> n;

	for (int i = 0; i < n - 1; i++)
	{
		left = arr
	}
	cout << right - left;
	return 0;
}