#include <iostream>
#include <vector>
using namespace std;

int main()
{
	ios_base::sync_with_stdio(0);
	int n, temp;
	cin >> n;
	vector <pair<int, int> > v;
	for (int i = 0; i < n; i++)
	{
		cin >> temp;
		v.push_back(make_pair(temp, 0));
	}
	for (int i = 0; i < n; i++)
	{
		   
	}
	return 0;
}